/** @deprecated Use `./auth` — kept for transitional imports. */
export {
  currentUser,
  setCursorModel,
  logoutBrowserSession,
  type AuthenticatedUser,
  type AuthMethod,
  type CursorModelOption,
} from './auth'
